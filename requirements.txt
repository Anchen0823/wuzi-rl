# ============================================================
# wuzi-rl 依赖白名单
# 自研项目：仅基础工具，不含任何 AI 模型 / 预训练权重 / AI 组件。
# PyTorch 仅作为 GPU 张量计算与自动微分引擎使用（见 docs/PLAN.md §1.2）。
# PyTorch 需安装对应 CUDA 的官方版本，安装命令以
#   https://pytorch.org/get-started/locally/ 为准。
# ============================================================

pygame>=2.6
numpy>=2.0
torch>=2.9
