# 五子棋 AI 项目计划书（wuzi-rl）

| 项 | 内容 |
|---|---|
| 版本 | v0.1（M0 初版，随里程碑迭代更新） |
| 仓库 | github.com/Anchen0823/wuzi-rl（公开） |

## 1. 项目概述

### 1.1 目标

1. 用 **pygame** 实现一个完整可玩的 15×15 五子棋游戏（人机对战、双人对战、AI 观战、回放）。
2. 训练一个**完全自研**的强化学习 AI：从零开始、仅靠自对弈学会下棋（AlphaZero 风格），不使用任何他人的开源 AI 模型、预训练权重、RL 库或现成棋类 AI 组件。
3. v1 棋力目标：稳定击败随机/贪心基线，并显著强于限时纯 MCTS 基线；训练管线可持续运行、后续继续增强。

### 1.2 自研边界（关键承诺）

| 类别 | 内容 | 归属 |
|---|---|---|
| 允许的基础设施 | Python 标准库、NumPy（数组运算）、pygame（图形/事件）、PyTorch 张量运算与自动微分、CUDA 运行时 | 通用计算/图形工具，非 AI 组件 |
| 完全自研 | 规则引擎、棋盘状态编码、对称变换、神经网络结构设计与实现、初始化方案、MCTS/PUCT 全部逻辑、自对弈管线、训练循环与损失函数、优化配置、评估系统、UI 与 AI 集成 | 本项目核心 |
| 明确禁止 | 任何预训练权重/模型文件；任何他人开源的棋类 AI、MCTS、AlphaZero/强化学习实现（不参考、不复制其代码）；stable-baselines 等 RL 库；torchvision/timm 等模型组件库 | — |

> PyTorch 在本项目中的角色：仅作为「GPU 张量计算 + 自动微分」引擎（相当于编译器/计算库）。所有 AI 逻辑与结构均为本项目自行设计实现，并以依赖白名单审计（`tools/dep_audit.py`，M1 落地）保证长期合规。

### 1.3 范围与非目标

- 范围内：15×15 标准五子棋（自由规则、无禁手）、AI 训练与评估、人机交互、训练数据与模型管理。
- v1 非目标：禁手/连珠规则、联机对战、Swap2 开局规则、多 GPU 分布式训练、发布安装包（M5 视情况决定）。

## 2. 本机环境现状（M0 实测）

| 项 | 实测值 | 说明 |
|---|---|---|
| GPU | NVIDIA GeForce RTX 4060 Laptop GPU，8188 MiB | 训练主算力 |
| 驱动 | 610.88 | 满足 CUDA 12/13 运行时要求 |
| CUDA 工具链 | nvcc release 13.3 | 已安装（v1 使用 PyTorch 自带 CUDA 运行时） |
| Python | 3.14.7 | 较新，需验证第三方 wheel 支持 |
| pygame / NumPy | 未安装 | M1 安装 |
| PyTorch | 未安装 | M1 按 CUDA 匹配安装 |
| git | 2.49.0，身份已配置 | 就绪 |
| GitHub CLI | 2.92.0，已登录 Anchen0823（repo 权限） | 远端操作就绪 |

## 3. 技术选型

| 组件 | 选型 | 用途 | 边界 |
|---|---|---|---|
| 语言 | Python 3.14（wheel 不兼容时回退 3.12/3.13 venv） | 全项目 | 基础 |
| 图形 | pygame ≥ 2.6 | 棋盘渲染、事件、交互 | 基础 |
| 数值 | NumPy ≥ 2.0 | 棋盘编码、增强、MCTS 内数据结构 | 基础 |
| 深度学习引擎 | PyTorch ≥ 2.9（CUDA 版） | 张量运算、自动微分、GPU 调度 | 基础设施 |
| 训练循环 | 自研（基于 torch 基础张量操作） | 损失/优化/评估门 | 自研 |
| 网络结构 | 自研残差 CNN（DESIGN.md §6） | 策略 + 价值双头 | 自研 |
| 搜索 | 自研 MCTS/PUCT（DESIGN.md §7） | 行棋决策 | 自研 |
| 数据/模型 | NumPy `.npz` / torch `.pt` | 训练数据、checkpoint | 自研格式 |

## 4. 总体架构

```
┌──────────────────────────────────────────────────────────────┐
│  pygame GUI（gomoku/gui.py）                                  │
│   人vs人 │ 人vsAI（选边/难度） │ AIvsAI 观战 │ 回放            │
└──────────────┬───────────────────────────────────────────────┘
               │ 落子 / 查询最佳着
┌──────────────▼───────────────────────────────────────────────┐
│  对局管理（gomoku/game.py）── 规则引擎（gomoku/board.py）      │
│  15×15、黑先、自由规则、胜负/和棋判定、悔棋、对称变换           │
└──────────────┬───────────────────────────────────────────────┘
               │ state（4×15×15 编码）
┌──────────────▼───────────────────────────────────────────────┐
│  AI 决策层                                                     │
│  ┌────────────────┐   ┌────────────────────────────────┐      │
│  │ MCTS + PUCT      │◄──│ 神经网络（自研 ResNet）         │      │
│  │ （ai/mcts.py）    │──►│ 策略头 π(225) + 价值头 v(±1)    │      │
│  └────────────────┘   │ （ai/net.py，仅用张量原语）       │      │
│  纯 MCTS 基线（M2）     └────────────────────────────────┘      │
└───────────────────────────────────────────────────────────────┘
```

训练管线（离线，与 GUI 解耦）：

```
自对弈（ai/selfplay.py）              训练（ai/train.py）
MCTS(当前最佳) ──对局──► (s, π, z) ──► 经验池(最近N局) ──► 采样/增强/梯度更新
      ▲                                                       │
      │                   评估门（ai/arena.py）                 ▼
      └── 胜率≥55% 则替换 ──── 新网 vs 当前最佳（200局换边） ── checkpoint
```

## 5. 里程碑计划

| 阶段 | 内容 | 交付物 | 验收标准 | 预估工时 |
|---|---|---|---|---|
| **M0** | 文档与仓库 | 计划书、设计文档、GitHub 仓库 | 文档完整、远端推送成功 | ✅ 本轮 |
| **M1** | 环境 + 规则引擎 + 界面 | 依赖安装与 CUDA 冒烟；`board.py` 全实现 + 单测；pygame 界面 | GPU 冒烟通过；单测全绿；人vs人完整下一局（含胜负判定/悔棋） | 1–2 天 |
| **M2** | 纯 MCTS 基线 AI | `mcts.py`（均匀先验 + 随机 rollout）；接入界面 | AI 单步 ≤ 2s，且能发现即刻连五/封堵 | 1–2 天 |
| **M3** | 网络 + 训练管线闭环 | `net.py`；selfplay/train/arena；数据与 checkpoint 管理 | 完成一次完整迭代（自对弈→训练→评估），损失下降、无崩溃 | 3–5 天 |
| **M4** | 强化训练 + 产品化接入 | 长时训练（GPU 挂机）；难度档；人机对战打磨 | 棋力验收达标（§7）；对局体验流畅 | 训练 3–7 天 GPU + 工程 2–3 天 |
| **M5** | 打磨与交付 | 全量测试、文档、依赖审计、可选打包 | §7 全部通过 | 2–3 天 |

## 6. 训练方案概要（详见 DESIGN.md）

- **范式**：AlphaZero 风格自对弈强化学习——MCTS 搜索产出策略目标 π，对局结果产出价值目标 z。
  - 搜索：PUCT，`Q(s,a) + c_puct · P(s,a) · √N(s) / (1 + N(s,a))`
  - 目标：终局以“当前玩家视角”归一化 z = ±1（满盘和棋 z = 0）。
- **数据**：自对弈每步记录 (状态, π, z)；D4 对称群 8 种增强；经验池保留最近 N 局并随机采样。
- **训练**：`L = (z − v)² − πᵀ log p + λ‖θ‖²`；策略/价值双头联合训练；每 K 步触发评估门。
- **评估**：新网 vs 当前最佳，强制黑白交换共 200 局，胜率 ≥ 55% 才采纳并更新“最佳”checkpoint。
- **算力预算（4060 8GB，估算）**：
  - 网络约 7.2M 参数（约 29MB），batch 512 训练显存远低于 8GB；
  - 自对弈约 2–5 秒/局（200 次模拟/步，批量叶子评估）→ 500 局/迭代约 20–40 分钟；
  - 预计 **2–5 天 GPU 时间**达到 v1 棋力目标（数十次迭代）；后续持续训练可继续增强。

## 7. 整体验收标准

| 类别 | 标准 |
|---|---|
| 功能 | 人vs人、人vsAI、AI 观战、回放均可完整使用；规则判定 100% 正确（单测覆盖） |
| 棋力 | vs 随机 ≥ 99%；vs 贪心启发式基线 ≥ 90%；vs 限时纯 MCTS（2s/步）≥ 70%（目标值，实测后校准） |
| 体验 | 中难度单步 ≤ 2s；训练期间可正常对弈（推理与训练互不阻塞） |
| 自研审计 | 依赖白名单通过；仓库内无任何外部模型文件/权重/复制代码 |
| 质量 | 核心模块单测覆盖率 ≥ 80%；训练管线可中断后一键续训 |

## 8. 风险与对策

| 风险 | 概率 | 影响 | 对策 |
|---|---|---|---|
| Python 3.14 下 pygame/torch 无可用 wheel | 低 | 中 | M1 首日验证；回退 3.12/3.13 venv（零代码改动） |
| CUDA/PyTorch 环境不匹配 | 低 | 中 | 用 torch 自带 CUDA 运行时；M1 做 GPU 张量冒烟测试 |
| 自对弈速度低于预算 | 中 | 高 | 批量叶子评估；降训练模拟数（200→100）；v2 多进程 worker + 集中 GPU 推理 |
| 训练不收敛 / 棋力天花板 | 中 | 高 | 调参矩阵（lr/噪声/温度/网络深度）；状态特征增强（上一步位置、活四/双三威胁层）；增大网络与模拟数 |
| 五子棋先手（黑）优势影响训练 | 高 | 中 | 对称增强天然平衡黑白；评估强制换边；后续可选 Swap2 |
| 训练周期超预期 | 中 | 中 | 目标分层：v1 先达“稳胜 MCTS 基线”，更强水平作为 v2 持续训练 |

## 9. 下一步（M1 任务清单）

1. 创建 venv 并安装 pygame / NumPy / PyTorch（CUDA 版），跑 GPU 冒烟测试。
2. 实现 `gomoku/board.py` 规则引擎 + 完整单测（胜负、边界、满盘、对称变换）。
3. 实现 pygame 界面：棋盘绘制、落子、胜负提示、悔棋、人vs人对局。
4. 落地 `tools/dep_audit.py` 依赖白名单审计，锁死自研边界。
